export const planExamHandler = (req, res, next) => {
    const number_of_rooms = req.body.number_of_rooms;
    const number_of_benches_per_room = req.body.number_of_benches;
    const number_of_students_per_bench = req.body.number_of_students;

    
}